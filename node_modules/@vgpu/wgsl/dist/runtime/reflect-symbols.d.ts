import type { ImportDecl } from "./parser.ts";
import type { MangleModule } from "./mangler.ts";
import type { ModuleSymbols, ParsedDecls, Registry, WGSLType } from "./reflect-types.ts";
/**
 * Resolves an import declaration to the canonical path of the imported module, using the same
 * module resolution that loaded the graph (relative/root-alias/`packageMap`/`package.json`
 * `exports`). `resolveShader()` passes its resolver so bare package specifiers resolve for
 * nominal type positions exactly like they do for value positions in the mangler.
 */
export type ImportPathResolver = (from: string, imp: ImportDecl) => string;
export declare function buildModuleSymbols(modules: readonly MangleModule[], parsed: readonly ParsedDecls[], resolveImport?: ImportPathResolver): ReadonlyMap<string, ModuleSymbols>;
export declare function buildRegistry(parsed: readonly ParsedDecls[], symbols: ReadonlyMap<string, ModuleSymbols>): Registry;
export declare function resolveType(type: WGSLType, path: string, symbols: ReadonlyMap<string, ModuleSymbols>, registry: Registry): WGSLType;
export declare function unwrapAlias(type: WGSLType, registry?: Registry): WGSLType;
export declare function resolveAliasesDeep(type: WGSLType, registry: Registry): WGSLType;
//# sourceMappingURL=reflect-symbols.d.ts.map
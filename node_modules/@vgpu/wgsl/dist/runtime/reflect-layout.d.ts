import { type HostShareableLayout, type Registry, type WGSLType } from "./reflect-types.ts";
/**
 * Calculates naga-standard host-shareable layout metadata for uniform/storage values.
 * `bool` is rejected because WGSL booleans are not host-shareable, and runtime arrays report
 * `runtimeSized` with no fixed byte size so callers can provide the final binding size manually.
 */
export declare function layoutOf(type: WGSLType, addressSpace: "uniform" | "storage", name?: string, mangledName?: string, registry?: Registry): HostShareableLayout;
//# sourceMappingURL=reflect-layout.d.ts.map
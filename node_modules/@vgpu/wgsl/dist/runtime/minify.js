import { wgslError } from "./errors.js";
import { scan } from "./scanner.js";
import { applyIdentifierMinifyWgsl } from "./identifier-minify.js";
import { printWgslTokens } from "./token-printer.js";
const defaultMinifyOptions = { whitespace: false, identifiers: "none" };
export function normalizeMinifyOption(option) {
    if (option === undefined || option === false)
        return defaultMinifyOptions;
    if (option === true)
        return { whitespace: true, identifiers: "safe" };
    const identifiers = option.identifiers ?? "none";
    if (identifiers !== "none" && identifiers !== "safe") {
        throw wgslError("VGPU-WGSL-MINIFY-IDENTIFIERS", `Unknown WGSL minify identifiers mode: ${String(identifiers)}`);
    }
    return { whitespace: option.whitespace ?? true, identifiers };
}
export function applyMinifyWgsl(source, option) {
    const minify = normalizeMinifyOption(option);
    if (minify.identifiers === "safe")
        return applyIdentifierMinifyWgsl(source, { whitespace: minify.whitespace }).wgsl;
    if (minify.whitespace)
        return minifyWgsl(source);
    return source;
}
export function minifyWgsl(source, options = {}) {
    try {
        return printWgslTokens(scan(source), options);
    }
    catch (error) {
        if (isWgslError(error) && error.code === "VGPU-WGSL-LEX-UNTERM-COMMENT") {
            throw wgslError("VGPU-WGSL-MINIFY-BLOCK", "Unterminated WGSL block comment", error.line, error.column);
        }
        throw error;
    }
}
function isWgslError(error) {
    return typeof error === "object" && error !== null && "code" in error && "line" in error && "column" in error;
}
//# sourceMappingURL=minify.js.map
import type { Diagnostic } from "./diagnostic-types.ts";
export interface PackageResolveOptions {
    readonly entry: string;
    readonly rootDir?: string;
    readonly packageMap?: Record<string, string>;
    readonly modules?: Record<string, string>;
}
/** Fix-it for a bare package specifier that is not installed. WGSL packages are npm packages: `@vgpu/wgsl-std` ships with `vgpu`, anything else has to be installed. */
export declare const PKG_NOTFOUND_FIXIT = "Install the package (npm install <pkg>) or check the specifier";
/** Fix-it for the in-memory (`modules`) resolver, where node_modules is never consulted. */
export declare const PKG_NOTFOUND_VIRTUAL_FIXIT = "Map it with packageMap or add the module to modules";
/** Package name of a bare specifier: `@scope/name/sub` -> `@scope/name`, `name/sub` -> `name`. */
export declare function packageNameOf(spec: string): string;
export declare function resolveImport(spec: string, from: string, opts: PackageResolveOptions, diagnostics: Diagnostic[]): string;
export declare function readModule(path: string, opts: PackageResolveOptions): Promise<string>;
export declare function canonicalEntry(entry: string, opts: PackageResolveOptions): string;
//# sourceMappingURL=package-resolution.d.ts.map
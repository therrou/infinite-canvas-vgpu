export interface RenameAllocatorOptions {
    readonly reserved?: Iterable<string>;
}
export declare class RenameAllocator {
    private nextIndex;
    private readonly used;
    constructor(options?: RenameAllocatorOptions);
    reserve(name: string): void;
    allocate(): string;
    private isAvailable;
}
export declare function candidateName(index: number): string;
//# sourceMappingURL=rename-allocator.d.ts.map
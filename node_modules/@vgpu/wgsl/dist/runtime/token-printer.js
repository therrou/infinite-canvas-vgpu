const commentKinds = new Set(["lineComment", "blockComment"]);
const commentJoinOperators = new Set([
    "==", "!=", "<=", ">=", "->", "&&", "||", "+=", "-=", "*=", "/=", "%=", "&=", "|=", "^=",
    "<<", ">>", "<<=", ">>=",
]);
const whitespaceAmbiguousOperators = new Set(["&&", "||", "++", "--", "<<", ">>", "<<=", ">>="]);
export function printWgslTokens(tokens, options = {}) {
    let out = "";
    let previous;
    let sawComment = false;
    for (let i = 0; i < tokens.length; i++) {
        const token = tokens[i];
        if (isComment(token)) {
            sawComment = true;
            continue;
        }
        const text = replacementText(token, i, options);
        if (text === "")
            continue;
        if (previous) {
            const gap = sawComment ? "comment" : previous.token.end < token.start ? "whitespace" : "none";
            if (needsSeparator(previous, { token, text }, gap))
                out += " ";
        }
        out += text;
        previous = { token, text };
        sawComment = false;
    }
    return out;
}
function replacementText(token, index, options) {
    const callback = options.replaceToken?.(token, index);
    if (callback !== undefined)
        return callback;
    const replacements = options.replacements;
    if (!replacements)
        return token.text;
    return replacements.get(token) ?? replacements.get(index) ?? replacements.get(token.text) ?? token.text;
}
function needsSeparator(previous, current, gap) {
    const left = previous.text;
    const right = current.text;
    const leftLast = left[left.length - 1] ?? "";
    const rightFirst = right[0] ?? "";
    if (isIdentContinue(leftLast) && isIdentContinue(rightFirst))
        return true;
    if (previous.token.kind === "number" && (leftLast === "e" || leftLast === "E" || leftLast === "p" || leftLast === "P") && (rightFirst === "+" || rightFirst === "-"))
        return true;
    if ((isIdentContinue(leftLast) || leftLast === ")" || leftLast === "]" || leftLast === ">") && rightFirst === "@")
        return true;
    if ((leftLast === ")" || leftLast === "]" || leftLast === ">" || leftLast === "\"" || leftLast === "'") && isIdentStart(rightFirst))
        return true;
    if (isDigit(leftLast) && rightFirst === ".")
        return true;
    if (leftLast === "." && isDigit(rightFirst))
        return true;
    if (isPunctuationToken(previous.token, left) && isPunctuationToken(current.token, right)) {
        return needsPunctuationSeparator(left, right, gap);
    }
    return false;
}
function needsPunctuationSeparator(left, right, gap) {
    const joined = left + right;
    if (gap === "comment" && commentJoinOperators.has(joined))
        return false;
    if (gap !== "none" && (left === ">" && right === ">"))
        return true;
    if (gap !== "none" && whitespaceAmbiguousOperators.has(joined))
        return true;
    if (gap !== "none" && left === "-" && right === ">")
        return true;
    return false;
}
function isComment(token) { return commentKinds.has(token.kind); }
function isPunctuationToken(token, text) { return token.kind === "punct" || /^[^A-Za-z0-9_\s]+$/.test(text); }
function isIdentStart(char) { return /[A-Za-z_]/.test(char); }
function isIdentContinue(char) { return /[A-Za-z0-9_]/.test(char); }
function isDigit(char) { return /[0-9]/.test(char); }
//# sourceMappingURL=token-printer.js.map
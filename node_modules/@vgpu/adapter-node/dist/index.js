import { readdirSync } from "node:fs";
import { Device, validateRequiredFeatures, VGPUError } from "@vgpu/core";
import { resolveWebGPU } from "./dawn-loader.js";
import { formatSoftwareFallbackNotice, isSoftwareAdapter, softwareAdapterName } from "./software-fallback-notice.js";
import { createPrivateSoftwareRendererCopy, getCachedSoftwareRenderer } from "./software-renderer-cache.js";
export function nodeAdapterEnvironmentOverride() {
    const value = process.env.VGPU_ADAPTER;
    if (value === undefined || value === "")
        return undefined;
    if (value !== "software" && value !== "hardware")
        throw new VGPUError({
            code: "VGPU-NODE-ADAPTER-INVALID",
            message: `Invalid VGPU_ADAPTER=${JSON.stringify(value)}; expected "software" or "hardware".`,
            fix: "Unset VGPU_ADAPTER or set it to software or hardware.",
            where: "init",
        });
    if (!announcedAdapterOverrides.has(value)) {
        announcedAdapterOverrides.add(value);
        console.error(`vgpu: adapter overridden by VGPU_ADAPTER=${value}`);
    }
    return value;
}
export function describeNodeAdapter(info) {
    const details = info;
    return { name: String(details?.description || details?.device || details?.vendor || "unknown adapter"), type: isSoftwareAdapter(details) ? "cpu" : "gpu" };
}
const defaultAdapterRequestRetryCount = 3;
const defaultAdapterRequestRetryBaseDelayMs = 100;
let dawnGPU = null;
let dawnFlagsUsed = null;
let loadedWebGPU = null;
let softwareOperation = Promise.resolve();
let announcedSoftwareFallback = false;
const announcedAdapterOverrides = new Set();
export function createNodeAdapter(options = {}) {
    return { requestDevice: (deviceOptions) => requestDevice(deviceOptions, options.adapter ?? "auto") };
}
export async function createNodeDevice(opts) {
    return requestDevice(opts, "auto");
}
async function requestDevice(opts = {}, mode) {
    const webgpu = await loadWebGPU();
    Object.assign(globalThis, webgpu.globals);
    const options = adapterOptions(opts);
    const vendorIcdPresent = hasVendorVulkanIcd();
    let adapter;
    let software = false;
    let fallbackReason = "cpu-adapter-selected";
    if (mode === "software") {
        const icd = requireCachedSoftwareRenderer();
        adapter = await withSoftwareIcd(icd, () => {
            dawnGPU = null;
            dawnFlagsUsed = null;
            return requestAdapterWithRetry(getDawnGPU(opts, webgpu, true), options, opts);
        });
        software = true;
    }
    else {
        const gpu = getDawnGPU(opts, webgpu);
        try {
            adapter = await requestAdapterWithRetry(gpu, options, opts);
            if (mode === "hardware" && isSoftwareAdapter(adapter.info)) {
                throw noAdapterError(new Error("Only a CPU software adapter was discovered."), "A real GPU was required by adapter: \"hardware\". Install or repair the vendor GPU driver, or choose adapter: \"auto\".");
            }
        }
        catch (error) {
            if (mode === "hardware" || !isNoAdapterError(error))
                throw error;
            const icd = getCachedSoftwareRenderer();
            if (!icd)
                throw noAdapterError(error, "Install the portable CPU renderer with `npx vgpu install-software-renderer`, then retry. Check the Mesa/driver version, Vulkan ICD (VK_ICD_FILENAMES), and display variables. To diagnose the environment, run: npx vgpu doctor");
            // The notice is emitted once the retry succeeds, so it lands *after* the native Dawn/Vulkan
            // startup lines it explains instead of being buried above them.
            fallbackReason = vendorIcdPresent ? "vendor-driver-failed" : "no-adapter";
            adapter = await withSoftwareIcd(icd, () => {
                // Vulkan ICD discovery is fixed when Dawn creates its native instance.
                // The hardware instance found nothing, so replace it for the consented retry.
                dawnGPU = null;
                dawnFlagsUsed = null;
                return requestAdapterWithRetry(getDawnGPU(opts, webgpu, true), options, opts);
            });
            software = true;
        }
    }
    validateRequiredFeatures(adapter.features, opts.requiredFeatures);
    const device = await adapter.requestDevice({ requiredFeatures: opts.requiredFeatures, requiredLimits: opts.requiredLimits });
    if (opts.label)
        device.label = opts.label;
    const info = software
        ? Object.assign({}, adapter.info, { description: adapter.info?.description || "lavapipe", adapterType: "cpu" })
        : adapter.info ?? null;
    if (mode === "auto")
        announceSoftwareFallback(info, fallbackReason);
    return new Device(device, info, { isCompatibilityMode: options.featureLevel === "compatibility" });
}
async function requestAdapterWithRetry(gpu, options, retryOptions) {
    let lastError = new Error("requestAdapter returned null");
    const maxAttempts = Math.max(1, retryOptions.adapterRequestRetryCount ?? defaultAdapterRequestRetryCount);
    const baseDelayMs = Math.max(0, retryOptions.adapterRequestRetryBaseDelayMs ?? defaultAdapterRequestRetryBaseDelayMs);
    for (let attempt = 1; attempt <= maxAttempts; attempt++) {
        try {
            const adapter = await gpu.requestAdapter(options);
            if (adapter)
                return adapter;
            lastError = new Error("requestAdapter returned null");
        }
        catch (error) {
            if (!shouldRetryAdapterRequestError(error))
                throw error;
            lastError = error;
        }
        if (attempt < maxAttempts)
            await sleep(baseDelayMs * 3 ** (attempt - 1));
    }
    throw noAdapterError(lastError);
}
function noAdapterError(cause, fix = "Check the Mesa/driver version, Vulkan ICD (VK_ICD_FILENAMES), and display variables (DISPLAY, WAYLAND_DISPLAY, XDG_RUNTIME_DIR). Use VGPU_DAWN_FLAGS to select a Dawn backend explicitly. Install a portable CPU renderer with `npx vgpu install-software-renderer`. To diagnose the environment, run: npx vgpu doctor") {
    return new VGPUError({
        code: "VGPU-NODE-NO-ADAPTER",
        message: `No WebGPU adapter available with Dawn flags [${dawnFlagsUsed?.join(",") ?? ""}].`,
        fix,
        where: "createNodeAdapter",
        cause,
    });
}
function isNoAdapterError(error) { return error instanceof VGPUError && error.code === "VGPU-NODE-NO-ADAPTER"; }
function announceSoftwareFallback(info, reason) {
    if (announcedSoftwareFallback || !isSoftwareAdapter(info))
        return;
    announcedSoftwareFallback = true;
    console.error(formatSoftwareFallbackNotice({ adapter: softwareAdapterName(info), reason }));
}
function requireCachedSoftwareRenderer() {
    const icd = getCachedSoftwareRenderer();
    if (icd)
        return icd;
    throw new VGPUError({
        code: "VGPU-NODE-SOFTWARE-RENDERER-MISSING",
        message: "The portable CPU software renderer is not installed.",
        fix: "Run `npx vgpu install-software-renderer`, then retry.",
        where: "createNodeAdapter",
    });
}
async function withSoftwareIcd(icd, operation) {
    const run = softwareOperation.then(async () => {
        const privateCopy = createPrivateSoftwareRendererCopy(icd);
        const previousIcd = process.env.VK_ICD_FILENAMES;
        const previousDrivers = process.env.VK_DRIVER_FILES;
        process.env.VK_ICD_FILENAMES = privateCopy.path;
        delete process.env.VK_DRIVER_FILES;
        try {
            return await operation();
        }
        finally {
            if (previousIcd === undefined)
                delete process.env.VK_ICD_FILENAMES;
            else
                process.env.VK_ICD_FILENAMES = previousIcd;
            if (previousDrivers === undefined)
                delete process.env.VK_DRIVER_FILES;
            else
                process.env.VK_DRIVER_FILES = previousDrivers;
            privateCopy.cleanup();
        }
    });
    softwareOperation = run.then(() => undefined, () => undefined);
    return run;
}
function hasVendorVulkanIcd() {
    if (process.env.VK_ICD_FILENAMES || process.env.VK_DRIVER_FILES)
        return true;
    try {
        return readdirSync("/usr/share/vulkan/icd.d").some((name) => name.endsWith(".json"));
    }
    catch {
        return false;
    }
}
function shouldRetryAdapterRequestError(error) { return !/AbortError/i.test(String(error)); }
function sleep(ms) { return new Promise((resolve) => setTimeout(resolve, ms)); }
function getDawnGPU(opts, webgpu, forceSoftware = false) {
    const flags = forceSoftware ? [] : backendFlags(opts);
    if (dawnGPU) {
        if (!flagsEqual(flags, dawnFlagsUsed))
            console.warn(`[@vgpu/adapter-node] Dawn already initialized with flags [${dawnFlagsUsed?.join(",") ?? ""}], ignoring requested flags [${flags.join(",")}]. Re-init causes SIGSEGV in Dawn.`);
        return dawnGPU;
    }
    dawnGPU = webgpu.create([...flags]);
    dawnFlagsUsed = [...flags];
    return dawnGPU;
}
async function loadWebGPU() { if (loadedWebGPU)
    return loadedWebGPU; loadedWebGPU = await resolveWebGPU(); return loadedWebGPU; }
function backendFlags(opts) {
    const envFlags = process.env.VGPU_DAWN_FLAGS?.split(/\s+/).filter(Boolean);
    if (envFlags && envFlags.length > 0)
        return envFlags;
    if (opts.backendFlags)
        return opts.backendFlags;
    if (opts.backend === "opengl")
        return ["backend=opengl"];
    if (process.platform === "linux" && opts.backend !== "webgpu" && (process.env.DISPLAY || process.env.WAYLAND_DISPLAY))
        return ["backend=opengl"];
    return [];
}
function adapterOptions(opts) { return { powerPreference: opts.powerPreference, ...(process.platform === "linux" && opts.backend !== "webgpu" ? { featureLevel: "compatibility" } : {}) }; }
function flagsEqual(a, b) { return b !== null && a.length === b.length && a.every((flag, index) => flag === b[index]); }
//# sourceMappingURL=index.js.map
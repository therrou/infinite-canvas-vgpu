import { type Device } from "@vgpu/core";
import type { BindingInfo, EntryPointInfo, Reflection } from "@vgpu/wgsl/reflect-source";
/** Builds explicit WebGPU BGL entries from the frozen ReflectionFacade bindingLayout metadata. */
export type BindingVisibilityFn = ((binding: BindingInfo) => GPUShaderStageFlags) & {
    readonly filterable?: ReadonlySet<string>;
};
/**
 * Stage visibility mask (plus the filtering-texture set) for the selected entry points.
 *
 * Driven strictly by each entry's reflected `bindings`/`samplingPairs`: a missing field throws
 * rather than falling back to `bindings` (which widened visibility to every declared resource) or
 * to `[]` (which downgraded filterable textures to `unfilterable-float`). `bindings` stays in the
 * signature as the module-wide binding list callers already hold; it is no longer a fallback.
 */
export declare function visibilityForEntries(_bindings: readonly BindingInfo[], entries: readonly EntryPointInfo[]): BindingVisibilityFn;
export declare function bindGroupLayoutEntriesForGroup(bindings: readonly BindingInfo[], group: number, visibility?: BindingVisibilityFn): GPUBindGroupLayoutEntry[];
export declare function bindGroupLayoutsForReflection(device: Device, label: string, reflection: Reflection, visibility?: (binding: BindingInfo) => GPUShaderStageFlags): ReadonlyMap<number, GPUBindGroupLayout>;
export declare function pipelineLayoutFor(device: Device, bindGroupLayouts: ReadonlyMap<number, GPUBindGroupLayout>): GPUPipelineLayout;
export declare function cachedBindGroupLayout(device: Device, label: string, entries: readonly GPUBindGroupLayoutEntry[]): GPUBindGroupLayout;
//# sourceMappingURL=set-layouts.d.ts.map
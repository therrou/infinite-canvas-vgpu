import type { Device } from "@vgpu/core";
import type { PingPongStorage, PingPongTargets, StorageAccess } from "./api-types.ts";
import type { Gpu } from "./kernel.ts";
import type { TargetTextureOptions } from "./target.ts";
import { OffscreenTarget } from "./target-offscreen.ts";
import { type RingStorageBuffer } from "./storage.ts";
/**
 * Two offscreen targets read/write swapped by `swap()`: the classic feedback pair for blur chains,
 * fluid solvers and any effect that samples the previous result. Both halves are destroyed by
 * `gpu.dispose()`.
 */
export declare function pingPong(gpu: Gpu, width: number, height: number, opts?: TargetTextureOptions): PingPongTargets;
/** Two storage buffers of `bytes` read/write swapped by `swap()`. Both halves are destroyed by `gpu.dispose()`. */
export declare function pingPongStorage(gpu: Gpu, bytes: number): PingPongStorage;
/** @internal `own` claims each half for a lifetime (the kernel's, from `pingPong()`); omitted, the halves are unowned. */
export declare function createPingPongTargets(device: Device, width: number, height: number, opts?: TargetTextureOptions, own?: (target: OffscreenTarget) => OffscreenTarget): PingPongTargets;
/** @internal See {@link createPingPongTargets} for `own`. */
export declare function createPingPongStorage(device: Device, bytes: number, access?: StorageAccess, own?: (buffer: RingStorageBuffer) => RingStorageBuffer): PingPongStorage;
//# sourceMappingURL=ping-pong.d.ts.map
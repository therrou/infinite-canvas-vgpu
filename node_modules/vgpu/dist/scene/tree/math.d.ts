export type Mat4 = Float32Array;
export declare function identityMat4(out: Mat4): Mat4;
export declare function copyMat4(out: Mat4, m: Mat4): Mat4;
/** out = position/quaternion/scale composed into a column-major TRS matrix. */
export declare function composeTrs(out: Mat4, position: Float32Array, quaternion: Float32Array, scale: Float32Array): Mat4;
/** out = a * b. Safe when out aliases a or b. */
export declare function multiplyMat4(out: Mat4, a: Mat4, b: Mat4): Mat4;
/** Inverts an affine TRS matrix (last row assumed 0,0,0,1). */
export declare function invertAffineMat4(out: Mat4, m: Mat4): Mat4;
/** out = m * [p, 1]; returns the transformed point. */
export declare function transformPoint(out: Float32Array, m: Mat4, p: Float32Array): Float32Array;
/** out = the linear part of m applied to a direction (no translation). */
export declare function transformDirection(out: Float32Array, m: Mat4, v: Float32Array): Float32Array;
/** Intrinsic XYZ Euler angles (radians) to quaternion. */
export declare function quatFromEuler(out: Float32Array, x: number, y: number, z: number): Float32Array;
/** out = a * b (quaternion product). Safe when out aliases a or b. */
export declare function multiplyQuat(out: Float32Array, a: Float32Array, b: Float32Array): Float32Array;
/** Conjugate of a unit quaternion (its inverse). */
export declare function conjugateQuat(out: Float32Array, q: Float32Array): Float32Array;
/** Quaternion from three orthonormal basis columns (rotation part of a matrix). */
export declare function quatFromBasis(out: Float32Array, m00: number, m01: number, m02: number, m10: number, m11: number, m12: number, m20: number, m21: number, m22: number): Float32Array;
/** Rotation that makes -Z point from eye toward target (camera/look convention). */
export declare function quatLookAt(out: Float32Array, eye: Float32Array, target: Float32Array, up: Float32Array): Float32Array;
/** Quaternion from the rotation part of an affine matrix, ignoring (positive) scale. */
export declare function quatFromMat4Rotation(out: Float32Array, m: Mat4): Float32Array;
//# sourceMappingURL=math.d.ts.map
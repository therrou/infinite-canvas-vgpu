import type { Mat4, Vec3 } from "./camera.ts";
/** Minimal camera-only matrix math, kept local so scene users do not load all of wgpu-matrix. */
export declare function viewProjection(projection: Mat4, eye: Vec3, target: Vec3, up: Vec3): Mat4;
export declare function perspective(fov: number, aspect: number, near: number, far: number): Mat4;
export declare function orthographic(left: number, right: number, bottom: number, top: number, near: number, far: number): Mat4;
//# sourceMappingURL=camera-math.d.ts.map
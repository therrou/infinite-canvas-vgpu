import { perspective, viewProjection } from "./camera-math.js";
const DEFAULT_UP = new Float32Array([0, 1, 0]);
export function perspectiveCamera(spec) {
    const projection = perspective(spec.fovYRadians, spec.aspect, spec.near, spec.far);
    const viewProjectionMatrix = viewProjection(projection, spec.position, spec.target, spec.up ?? DEFAULT_UP);
    const position = new Float32Array(spec.position);
    return Object.freeze({ viewProjectionMatrix, position });
}
//# sourceMappingURL=perspective-camera.js.map
export function cachedMesh(cache, device, key, build) {
    let meshes = cache.get(device);
    if (!meshes) {
        meshes = new Map();
        cache.set(device, meshes);
    }
    const cached = meshes.get(key);
    if (cached)
        return cached;
    const mesh = build();
    meshes.set(key, mesh);
    return mesh;
}
//# sourceMappingURL=mesh-cache.js.map
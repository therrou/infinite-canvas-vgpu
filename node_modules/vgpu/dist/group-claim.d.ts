import { Draw } from "./draw.ts";
export interface GroupClaim {
    readonly draw: Draw;
    readonly group: number;
    readonly bindGroup: GPUBindGroup;
}
/** Claims a reflected bind group on a Draw. Dynamic offsets still belong to p.draw(draw, { offsets }). */
export declare function claimGroup(draw: Draw, group: number, bindGroup: GPUBindGroup): GroupClaim;
//# sourceMappingURL=group-claim.d.ts.map